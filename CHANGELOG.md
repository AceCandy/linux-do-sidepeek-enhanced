# 更新日志

这个文件只记录会影响实际使用体验的变化。

## [1.0.0] - 2026-09-23

- 新增收藏管理：等级下方悬浮入口，紧凑列表与完整管理面板，帖子及回复楼层可直接在侧栏阅读。
- 支持收藏夹、拖拽排序与原位改名、标签、备注、搜索高亮，以及按账号隔离的备份导入导出。
- 标题和楼层收藏按钮悬停选择分类，已有收藏可移动或直接取消；设置中可关闭增强收藏，保留直接收藏能力和整理数据。
- 接入 GitHub Gist 手动同步分类、顺序、标签与备注，支持冲突检测和同步前备份；Token 行内输入不回填，问号提供申请和使用指引。
- 新建、拖拽等本地整理不逐次请求账号接口；已验证会话内悬停直接显示一小时缓存，不清空列表，新增/取消后刷新，首次/会话变化/过期/手动刷新时联网。
- 优化主题与回复层级、分类和标签区分、清空搜索与明亮高亮、桌面和手机布局；取消收藏不再弹窗，其余确认统一样式。
- 扩展新增本机存储及 GitHub Gist 权限，油猴同步适配；非公开 Gist 不加密，Token 仅存本机，页面内输入仍可能被原站脚本读取。
- 静态检查、收藏与智能预览固定页面回归通过；真实账号、Firefox 与油猴管理器实装尚未验证。Firefox 发布包仍为未签名版本。

## [0.8.6] - 2026-09-23

- 帖内搜索在同帖楼层跳转、刷新及打开设置或回复时保持在顶部，保留输入、结果和分页；手动关闭、切帖或关闭侧栏时清理。
- 搜索结果的未加载楼层改为在当前侧栏补取并定位，不再自动打开新标签；仅首帖及仅楼主模式也可定位目标。
- 搜索摘要与正文增加关键词高亮，支持多词、大小写和特殊字符，正文重绘后恢复高亮；不支持原生高亮的旧浏览器保留搜索与定位。
- 扩展与油猴脚本同步更新；静态检查、固定页面浏览器回归通过，真实站点、Firefox 和油猴管理器实装尚未验证。

## [0.8.5] - 2026-09-22

- 右下角第一排调整为“刷新 + 帖内搜索”，第二排保留“回到顶部 + 跳到最新回复”，下方保留回复入口。
- 新增当前整帖搜索与结果分页，覆盖尚未加载的楼层；已显示楼层在抽屉内定位，未显示楼层在新标签打开。
- 搜索支持 Esc 关闭、无结果与失败提示；新搜索、切帖及关闭抽屉取消旧请求，避免过期结果串入。
- 扩展与油猴脚本同步更新，补充桌面、390px 窄屏、结果滚动、分页、文本安全和请求取消回归。
- 静态检查与固定页面浏览器回归通过；真实站点因 403 未验证，Firefox 和油猴管理器实装尚未验证。

## [0.8.4] - 2026-09-21

- 修复 Alt/Option + 上下键切帖或定位楼层后，首帖顶部被标题栏遮住且无法向上滚动恢复的问题。
- 禁止抽屉外层参与滚动，保留正文滚动、楼层定位和回到顶部；扩展与油猴脚本同步更新。
- 补充正文溢出、长短帖连续切换、末楼定位及回顶回归，覆盖宽屏和窄屏；旧样式可复现问题，修复后通过。
- 静态检查与固定页面浏览器回归通过；真实站点完整交互、Firefox 和油猴管理器实装尚未验证。

## [0.8.3] - 2026-09-14

- 修复超长帖分页请求返回后回到旧滚动位置的问题，网络请求期间继续滑动也能保持当前位置。
- 右下角新增“跳到最新回复”按钮；未完整加载的超长帖使用 `/last` 窗口直接定位末楼，不一次性加载全部中间回复。
- 智能预览头像接入 Discourse 原生用户卡片；修饰键点击和原生资料页链接保持可用。
- 扩展与油猴脚本同步更新，补充长帖滚动、末楼定位、头像用户卡片与浮层交互回归。
- 静态检查与固定页面浏览器回归通过；真实 linux.do 超长帖、用户卡片内容、Firefox 和油猴管理器实装尚未验证。

## [0.8.2] - 2026-09-12

- 修复带楼层定位的长帖禁用后续分页的问题，保留定位并允许继续下滑补齐回复；刷新后无需关闭重开即可查看新增楼层。
- 刷新绕过进行中的预取，主题 JSON 请求禁用 HTTP 缓存，重新获取服务器数据。
- 分页接口返回空结果时恢复加载状态并提示重试，避免一直卡在加载中。
- 移除底部重复的预览数量提示，保留分页加载状态；油猴脚本同步更新。
- 静态检查与固定页面浏览器回归通过，覆盖 35 / 41 条定位窗口刷新到 43 条、空分页重试及楼层去重排序；真实帖子、Firefox 和油猴管理器在线交互尚未验证。

## [0.8.1] - 2026-09-12

- 首页搜索、顶栏搜索下拉框和独立搜索页中的帖子标题支持侧栏预览，保留楼层路径；非帖子链接和新标签操作保持原样。
- 点击搜索下拉框中的帖子后自动收起下拉框，保留搜索词及再次展开能力；独立搜索页结果列表不隐藏。
- “宽屏帖子列表靠左”同时支持独立搜索页，去除搜索框、结果数量和列表内层多余留白，保留原站导航栏与悬浮模式。
- 补充搜索入口、原生操作放行、下拉框收起及实际内容左边界回归检查；油猴脚本同步更新。
- 静态检查和固定页面浏览器回归通过，已核对原站搜索布局及收起机制；Firefox 与油猴管理器完整实装交互仍未验证。

## [0.8.0] - 2026-09-12

### 预取与阅读

- 可见主题每秒最多启动 2 个预取请求，并发最多 2 个；后台页面及整页模式暂停预取。
- 预取与已打开主题共用最多 100 个主题的内存缓存，10 分钟过期；同主题楼层共享正文，分别保留阅读位置。
- 自动/智能模式按实际可见楼层上报阅读时间，可在设置中关闭；预取不计已读，整页模式由原站处理。

### 正文与操作

- 智能预览复用原站正文装饰、轮播、图片放大及 Boost 组件；模块不可用时保留回退路径。
- 楼层统计与操作合并为一排，表情反应使用单入口，修复长代码撑宽正文的问题。
- 刷新、回顶、回复改为右下悬浮入口；标签并入主题信息，阅读说明只浮现一次且可关闭。

### 顶栏与设置

- 原站边栏按钮和 Logo 靠左，抽屉顶部留出左上缺口；原站入口仍属于原站顶栏，随抽屉收放调整位置。
- 头像菜单内通知、回复、点赞对应的帖子链接改为侧栏预览，保留楼层路径并自动收起菜单；其他链接和新标签操作不受影响。
- 新增紧凑信任等级面板，支持悬停展开、点击固定、手动刷新和隐藏；新增 Connect 访问权限。
- 宽屏列表靠左可独立开启，不改变悬浮模式；保留原站边栏开关。设置弹窗居中限高、内部滚动。

### 发行与兼容

- 油猴脚本由公共源码与模板生成，提供同步校验及固定页面回归；继续保留油猴存储迁移与降级。
- 发布 Chrome/Edge ZIP、Firefox 未签名 XPI 和 `.user.js`；Firefox 使用独立后台声明。
- 原站组件依赖 Discourse 内部模块，站点升级可能触发回退；最终已读、等级统计和通知红点由站点决定。
- 未完成 Firefox、油猴管理器完整交互及真实回复、上传、Boost 提交回归。

## [0.7.2] - 2026-09-11

### 二开版发布标识
- 项目更名为 Linux.do SidePeek Enhanced，明确上游来源与 AceCandy 的二开维护身份。
- 油猴脚本使用独立命名空间和仓库主页，移除上游更新地址与自定义更新提示，避免跨版本更新覆盖。
- 提供 Chrome/Edge ZIP、Firefox 未签名 XPI 和独立油猴文件下载。

### 本次改动

#### 1. 去掉左侧固定工具栏
- 删除原先占用横向空间的整列侧边工具栏。
- 帖子内容区域现在可以完整使用抽屉宽度。
- 抽屉左边缘仍保留很窄的拖拽热区，用于调整抽屉宽度，但不会占据正文空间。

#### 2. 工具按钮移动到顶部标题栏
- “上一帖 / 下一帖 / 刷新 / 设置 / 回复 / 新标签打开 / 关闭”全部移动到顶部。
- 设置按钮与“新标签打开”按钮位于 `LINUX DO 预览` 这一行右侧。
- 顶部按钮改成紧凑图标样式，并保留悬停提示。
- 窄屏/手机下自动隐藏上一帖、下一帖和顶部回复按钮，仍保留悬浮回复入口，避免标题栏拥挤。

#### 3. 最近帖子内存缓存
- 智能/自动预览会缓存最近 12 个已打开主题。
- 缓存最长保留 10 分钟，仅存在当前页面内存中，不写入磁盘。
- 关闭侧栏后再次打开同一个帖子，会优先直接从缓存恢复，不再先等待网络请求。
- 同时恢复上次在侧栏里的阅读滚动位置。
- 缓存数据距离最近一次网络获取超过 30 秒时，会先立即显示缓存内容，再静默刷新最新内容。
- 切换帖子时也会缓存刚刚阅读的帖子，因此在几个帖子之间来回查看会更快。

### 兼容说明
- 设置持久化逻辑沿用 0.7.1，不会清除已有设置。
- iframe“整页模式”仍主要依赖浏览器自身缓存；本次最近主题快速恢复主要针对“自动/智能预览”。
- 已通过 `node --check` JavaScript 语法检查。

### 仓库同步
- 同步完整 0.7.2 油猴脚本到 `userscript/linuxdo-sidepeek.user.js`，并将样式与逻辑拆分为扩展源文件。
- 扩展版继续使用站点 localStorage 保存设置，移除仅适用于油猴的 GreasyFork 更新提示；不增加扩展权限。
- 扩展隔离环境中，表情列表使用脚本已有的同源接口回退，图片使用标准表情路径回退。

## [0.6.0] - 2026-03-21

### 新增
- 支持抽屉快速回复框直接粘贴图片自动上传；成功后会插入与站点原生一致的 `upload://` Markdown。
- 支持作者过滤；在智能预览里可以切到 `只看楼主`，快速聚焦楼主回复。
- 主题级回复入口默认放到头部工具栏；设置中可单独开启或关闭右侧悬浮回复入口。
- 设置面板新增 `正文字号` 滑杆；智能预览下可即时调整帖子正文和 `pre` / `code` 的字号。

### 改进
- 修复抽屉内容滚到底后继续滚轮会带动外层页面继续滚动的问题，并补齐内层滚动容器的滚动链拦截与滚动条样式。
- 修复抽屉头部工具栏在操作项较多时容易挤成竖排窄条的问题，头部按钮布局更稳定。
- Firefox 未签名包补齐 `browser_specific_settings.gecko.data_collection_permissions.required = ["none"]` 元数据，发布产物检查同步收紧。
- 调整默认设置为 `整页模式`、`完整主题`、`15px` 正文、`全部作者`、`默认顺序`、悬浮回复入口关闭、`浮层模式` 和 `窄` 宽度。
- 修复整页模式下正文字号设置的禁用与生效边界；当前模式不会误改 iframe 里的字号。

### 致谢
- 感谢 PR 贡献者 `majorcheng` 带来了抽屉快速回复粘贴图片自动上传。
- 感谢 PR 贡献者 `shengmingboai` 带来了抽屉滚动条样式和滚动穿透修复。

## [0.5.0] - 2026-03-20

### 新增
- 支持抽屉模式设置；现在可以在 `挤压模式` 和 `浮层模式` 之间切换，浮层模式下支持点击抽屉外区域直接关闭。
- 支持在“首帖 + 最新回复”视图下手动刷新最新回复，刷新时会尽量保留当前抽屉滚动位置，不必关闭后重新打开主题。
- GitHub Release 现在会同时产出 Chrome ZIP 和 Firefox 未签名 XPI，方便在 Firefox 里做临时安装和验证。

### 改进
- 列表页“查看 x 个新的或更新的话题”提示条会固定在中间栏顶部区域，点击后回顶刷新更稳定。
- 抽屉内为楼主回复增加 `Topic Owner` 标记，阅读长帖时更容易识别楼主发言。
- 修复浮层模式下抽屉打开时点击另一个列表主题会先收起、第二次才打开的问题；现在会直接切换到新主题。

### 致谢
- 感谢 PR 贡献者 `shengmingboai` 带来了抽屉模式设置（挤压模式 / 浮层模式）。
- 感谢 PR 贡献者 `majorcheng` 带来了新话题提示条交互修复、`Topic Owner` 标记和最新回复刷新按钮。

## [0.4.0] - 2026-03-15

### 新增
- 支持在抽屉内直接回复主题或指定楼层；点击帖子卡片下方「回复这条」可以针对性回复，点击浮动「回复」按钮可以回复整个主题。支持 Markdown，`Ctrl+Enter` / `Cmd+Enter` 快捷发送。
- 支持无限滚动自动加载更多帖子；在"完整主题 + 默认顺序"模式下，下滑接近底部会自动拉取后续回复，底部显示加载进度。
- 加载更多时保持当前滚动位置，不会跳回顶部。
- 发送回复成功后，新帖子会自动合入当前视图并滚动定位到新帖。

### 改进
- 合并帖子流（post stream）时同步合并 `stream` 数组和 `posts_count`，为增量加载提供完整索引。
- 主题 URL 归一化改用 `parseTopicPath` 解析，路径截取更准确。
- 设置加载改为逐字段校验白名单，避免旧存储里的脏值污染新设置。
- JSON 预览失败时自动回退为 iframe 整页预览，不再显示纯文案错误。

### 致谢
- 感谢 linux.do 群友 `zhoudashuai` 贡献了抽屉内回复和无限滚动加载的初始实现，本版本基于其提交的改动合并而来。

## [0.3.0] - 2026-03-10

### 新增
- 支持抽屉内图片点击全屏放大预览；点击遮罩层、关闭按钮或按 `Esc` 可退出，不再默认跳到独立图片页。

### 修复
- 修复复用同一个抽屉切换到其他主题时，旧图片预览遮罩可能残留在新内容上方的问题。
- 修复带普通外链的图片在抽屉里被误拦截的问题；现在只有图片本身或其原图链接会触发抽屉内预览，普通链接仍按原目标打开。

### 致谢
- 感谢 linux.do 群友 `zhoudashuai` 提出“侧边栏图片点击后改成抽屉内全屏放大、点击遮罩层退出”的优化建议，并完成了这次体验补齐与相关修复。

## [0.2.0] - 2026-03-08

### 修复
- 修复“侧边预览不计浏览”的问题；现在智能预览和整页模式都会尽量按站点正常进帖的方式触发论坛计数。
- 修复切换智能预览 / 整页模式时可能重复计浏览的问题；现在同一次抽屉会话里，同一主题不会因为切模式被重复计数。
- 修复部分页面“右侧滑窗不弹出”的问题；现在首页、搜索页、分类页、标签页、用户发帖流等更多带主题链接的页面都可以直接侧边预览。
- 修复主题详情页里的推荐主题等标题链接不能侧边预览的问题；现在只排除正文、编辑器和弹层里的链接，不会误伤正常主题标题。
- 修复某些场景下误触发右侧滑窗的问题；帖子正文、编辑器、弹层菜单等区域里的主题链接默认不会被接管。
- 修复未读帖、搜索命中等链接总是从首帖打开的问题；现在会尽量保留你点击时对应的楼层位置。
- 修复深链接在“仅首帖”或“首帖 + 最新回复”模式下内容错位的问题；现在不会再把某条回复误当成首帖固定在顶部。
- 修复 `/last`、未读定位等 helper 链接在智能预览里丢失目标位置的问题；现在会尽量保留你点击时的最后位置或目标楼层。
- 修复部分主题链接识别错乱的问题；短格式楼层链接、搜索结果、用户动态里的同主题多条记录，现在不会再跳错帖或让“上一帖/下一帖”定位错乱。

### 体验调整
- 调整回复排序的产品定义；智能预览默认仍是正常顺序，“首帖 + 最新回复”现在明确表示优先展示最新一批回复，不再暗示整帖完整倒序。
- 调整重复点击同一帖子的处理方式；扩展不再自己做长时间去重，是否计入浏览交给论坛本身判断。
- 调整右侧滑窗的适用范围说明；现在强调的是“支持站内可预览的主题链接”，不再只写传统列表页。

## [0.1.0] - 2026-03-07

### 首次发布
- 支持在 Linux.do 右侧滑窗预览帖子，不用频繁进出帖子页。
- 支持智能预览与整页模式切换。
- 支持上一帖、下一帖和快捷键切换。
- 支持预览选项、抽屉宽度调整与设置保存。
